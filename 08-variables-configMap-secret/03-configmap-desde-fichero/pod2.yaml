apiVersion: v1
kind: Pod
metadata:
  name: pod1
spec:
  containers:
    - name: test-container
      image: busybox:latest
      command: ["sleep","3600"]
      envFrom:
      - configMapRef:
          name: datos-mysql-env
  restartPolicy: Never
